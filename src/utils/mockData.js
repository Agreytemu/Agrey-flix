export const generateMocks = (count, baseTitle, type = 'movie') => {
  return [];
};

export const MOCK_MOVIES = [];
export const MOCK_SERIES = [];
export const MOCK_ANIMATIONS = [];
export const MOCK_TRENDING = [];
export const MOCK_AFRICAN_MOVIES = [];

